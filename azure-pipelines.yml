# Code Generated by Sidekick is for learning and experimentation purposes only.
trigger:
  - main

pool:
  vmImage: ubuntu-latest

stages:
  - stage: Build
    jobs:
      - job: FirstJob
        steps:
          - bash: echo $(PipelineLevelVariable)
      - job: SecondJob
        steps:
          - bash: echo "build second job"

  - stage: DevDeploy
    variables: 
      environment: Dev
    dependsOn: Build
    jobs:
      - job: DevDeployJob
        steps:
          - bash: echo "build $(environment) devdeploy job"

  - stage: QADeploy
    dependsOn: Build
    jobs:
      - job: QAJob
        steps:
          - bash: echo "build qa job"

  - stage: ProdDeploy
    dependsOn:
    - DevDeploy
    - QADeploy
    jobs:
      - job: ProdDeployJob
        steps:
          - bash: echo "build prod deploy job"
